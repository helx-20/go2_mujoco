## NOTES
# Note: align these parameters with Isaac Lab training config. Parameter correspondences:
# - kps, kds: correspond to `stiffness` and `damping` in `source/isaaclab_assets/isaaclab_assets/robots/unitree.py`
# - default_angles: see `init_state` in `source/isaaclab_assets/isaaclab_assets/robots/unitree.py`
# - lin_vel_scale, ang_vel_scale, dof_pos_scale, dof_vel_scale: see `ObsTerm` in velocity_env_cfg.py, default `1`
# - action_scale: reduced to `0.25` in rough_env_cfg.py
# - cmd_scale: corresponds to `[lin_vel_x_scale, lin_vel_y_scale, ang_vel_scale]`, default `[1,1,1]`
## delete this later

xml_path: "deploy_mujoco/robots/go2/scene_terrain.xml"

# Total simulation time
simulation_duration: 60.0
# Simulation time step
simulation_dt: 0.002
# Controller update frequency (meets the requirement of simulation_dt * controll_decimation=0.02; 50Hz)
control_decimation: 10 # 0.02
policy_decimation: 20
lock_camera: True

kps: [20, 20, 20, 20, 20, 20, 20, 20, 20, 20, 20, 20]
kds: [0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5]

default_angles: [0.1, 0.8, -1.5, -0.1, 0.8, -1.5,
                 0.1, 1.0, -1.5, -0.1, 1.0, -1.5]

lin_vel_scale: 2.0
ang_vel_scale: 0.25
dof_pos_scale: 1.0
dof_vel_scale: 0.05
action_scale: 0.25
cmd_scale: [2.0, 2.0, 0.25]
num_actions: 12
#num_obs: 45
num_obs: 48

# isaaclab default set heading_command True
# our pre_trained model use heading_command True so you may retrain policy if you want to use heading_command False
# heading_command: False # using cmd_init[2] directly controll yaw speed
# heading_command: True  # use the heading_target controll target yaw (cmd_init[2] is ignored)
heading_command: True
heading_target: 1.0 # yaw target in [-pi, pi] (facing to)
heading_stiffness: 0

cmd_init: [1.0, 0, 0] # move forward [-1,1]
# cmd_init: [0, 0.5, 0] # pan
# cmd_init: [0, 0, 0.5] # turn around
# cmd_init: [0, 0, 0]   # stop

init_skip_time: 2
